{
  "name": "heroku-docker-flex-gateway-example",
  "description": "A barebones Flex Gateway installation using Docker",
  "repository": "https://github.com/emanuelr93/heroku-docker-flex-gateway-example/blob/master/app.json",
  "keywords": ["flex", "gateway", "mulesoft", "docker", "proxy"],
  "stack": "container",
  "website": "https://docs.mulesoft.com/gateway/flex-gateway-overview",
  "env": {
    "GATEWAY_NAME": {
      "description": "Name of the Flex Gateway",
      "required": true
    },
    "PLATFORM_CONF": {
      "description": ".conf file from register step in base64",
      "required": true
    },
    "PLATFORM_PEM": {
      "description": ".pem file from register step in base64",
      "required": true
    },
    "PLATFORM_KEY": {
      "description": ".key file from register step in base64",
      "required": true
    }
  }
}
